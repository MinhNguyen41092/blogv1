<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
       <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>

    <div class="collapse navbar-collapse" id="navbar-collapse">
      <ul class="nav navbar-nav navbar-right">

        <li class="dropdown">
          <% if logged_in? %>
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= current_user.username %><span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to "View Profile", user_path(current_user) %></li>
              <li><%= link_to "Logout", logout_path %></li>
            </ul>
          <% else %>
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false" id="subscription">Subscription <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to "Subscribe", register_path %></li>
              <li><%= link_to "Login", login_path %></li>
            </ul>
          <% end %>
        </li>
      </ul>

      <div class="nav navbar-nav navbar-right">
        <div class="input-group">
          <%= form_tag(articles_path, method: :get, class: "navbar-form") do %>
            <%= text_field_tag 'search', params[:search], class: 'form-control', placeholder: 'Search'  %>
            <button class="btn btn-default" type="submit">
              <i class="glyphicon glyphicon-search"></i>
            </button>
          <% end %>
        </div>
      </div>

    <!-- Collect the nav links, forms, and other content for toggling -->

      <ul class="nav navbar-nav ">
        <li><%= link_to "About", about_path %> <span class="sr-only">(current)</span></li>
        <li><%= link_to "Articles", articles_path %> <span class="sr-only">(current)</span></li>
        <li><%= link_to "Projects", projects_path %> <span class="sr-only">(current)</span></li>

        <% if logged_in? %>
          <li><%= link_to "Write Your Article", new_article_path %> <span class="sr-only">(current)</span></li>
        <% end %>

      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
